const state = {
    counter: 0,
    message: 'Counter Message',
}

const getters = {}

const mutations = {}

const actions = {}

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions
}
